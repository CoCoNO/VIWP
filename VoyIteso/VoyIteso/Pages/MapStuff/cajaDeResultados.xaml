<UserControl x:Class="VoyIteso.Pages.MapStuff.cajaDeResultados"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="480" d:DesignWidth="480">

    <Grid x:Name="LayoutRoot" Background="{StaticResource PhoneChromeBrush}">
        <Grid Background="Green" Width="440" Height="150" Margin="30,168,10,162">

            <!--
            <Grid Margin="10,0,324,0">
                <Image Source="../../Images/dummy.png"></Image>
            </Grid>
            -->
            <StackPanel Margin="10,10,10,0" Height="140" VerticalAlignment="Top">
                <TextBlock x:Name="NombreDelConductor" Text="nombre del q da ride" FontSize="15"/>
                <TextBlock x:Name="DescripcionDeRuta" Text="detalles de la ruta encontrada" Height="114"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>