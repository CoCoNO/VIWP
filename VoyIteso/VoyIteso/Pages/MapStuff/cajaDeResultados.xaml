<UserControl x:Class="VoyIteso.Pages.MapStuff.cajaDeResultados"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="150" d:DesignWidth="440">

    <Grid x:Name="LayoutRoot" Background="{StaticResource PhoneChromeBrush}">
        <Grid Background="#1E81C0" Width="440" Height="150">

            <!--
            <Grid Margin="10,0,324,0">
                <Image Source="../../Images/dummy.png"></Image>
            </Grid>
            -->
            <StackPanel Margin="10,10,10,0" Height="140" VerticalAlignment="Top">
                <TextBlock x:Name="NombreDelConductor" Text="cargando..." FontSize="15"/>
                <TextBlock x:Name="DescripcionDeRuta" Text="" Height="114"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>